defaults:
- train_libero
- _self_
ckpt: null
gpu_id: 0
model:
  load_text_encoder: true
  skip_dit_load_from_pretrain: true
  action_dit_pretrained_path: null
EVALUATION:
  task_suite_name: libero_spatial
  task_id: 0
  num_trials: 50
  worker_task_file: null
  output_dir: ./runs/eval/libero/${now:%Y%m%d_%H%M%S}
  env_num: 1
  num_steps_wait: 30
  replan_steps: 10
  binarize_gripper: true
  use_action_ensembler: false
  action_horizon: 32
  num_inference_steps: 10
  sigma_shift: null
  text_cfg_scale: 1.0
  negative_prompt: ''
  rand_device: cpu
  tiled: false
  dataset_stats_path: null
  device: cuda
MULTIRUN:
  create_only: false
  task_file: null
  task_suite_names:
  - libero_10
  - libero_goal
  - libero_spatial
  - libero_object
  num_gpus: 8
  max_tasks_per_gpu: 2
